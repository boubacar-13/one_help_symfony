{{ form_start(myForm) }}
{{ form_widget(myForm) }}

<input type="submit" value="{{submitButtonText ?? 'Create Action' }}" class="btn btn-primary btn-lg btn-block" formnovalidate>
{{ form_end(myForm) }}