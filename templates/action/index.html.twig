{% extends 'base.html.twig' %}

{% block title 'One Help - Actions' %}

{% block body %}
{# <h1>{{ pluralize(action|length, 'Actions') }}</h1> #}
<h1>Les Actions</h1>
{% for action in actions %}
<article>
  <h2><a href="{{ path('app_action_show', {id: action.id}) }}">{{action.adresseMaraude}}</a></h2>
  <p>{{action.adresseMaraude}}</p>
  <p>{{action.villeMaraude}}</p>
</article>
{% endfor %}
{% endblock %}